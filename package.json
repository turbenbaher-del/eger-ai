{
  "name": "eger-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static.js",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting --project eger-ai"
  },
  "dependencies": {
    "firebase": "9.22.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
